# 配置数据源，这里目前是运行在IP地址为8.130.12.168的云服务器上的数据库
spring:
  datasource:
    url: jdbc:mysql://8.130.12.168:3306/hetadb?useUnicode=yes&characterEncoding=UTF-8&allowMultiQueries=true
    username: root
    password: 735568
    driver-class-name: com.mysql.cj.jdbc.Driver

# 配置Mybatis映射
mybatis:
  mapper-locations: classpath:/Mapper/*.xml
  type-aliases-package: com.heta.entity